namespace Carbon.Commerce.Tests
{
	using System;

	using Carbon.Models;

	using NUnit.Framework;

	[TestFixture]
	public class PackageTest
	{
		/*
		[Test]
		public void TotalPoundsToPoundsAndOunces()
		{
			var package = new Package {
				Weight = Weight.FromPounds(5.5d)
			};

			Assert.AreEqual(5, package.Weight.Pounds);
			Assert.AreEqual(8, package.Weight.Ounces);

			package.Weight = Weight.FromPounds(5.0d);

			Assert.AreEqual(5, package.Weight.Pounds);
			Assert.AreEqual(0, package.Weight.Ounces);

			package.Weight = Weight.FromPounds(1000);

			Assert.AreEqual(1000, package.Weight.Pounds);
			Assert.AreEqual(0, package.Weight.Ounces);

		}
		*/
	}
}
